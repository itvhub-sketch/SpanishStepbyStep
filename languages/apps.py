"""
The CardmakerConfig class is an AppConfig subclass that configures the 'cardDesigner' Django app. It sets the name of the app to 'cardDesigner'.
"""
from django.apps import AppConfig


class CardmakerConfig(AppConfig):
    name = 'cardDesigner'